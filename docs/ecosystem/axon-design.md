# Axon Protocol 技术设计文档

> 作者：qc
> 日期：2026-04-22
> 核心：MPC + Account Abstraction——非托管内核，Web2 体验层

---

## 一、定位与边界

### Axon 是什么

```
Web3 全场景极简交互中枢

对用户      零门槛，无需懂链/Gas/授权，像微信支付一样顺滑
对开发者    统一抽象接口，屏蔽多链差异，4 个核心接口搞定所有操作
底层联动    基于 web3-blitz 实现链交互、签名、跨链、兑换核心能力
```

### Axon 不是什么

```
不是托管钱包
    平台不持有完整私钥
    用户始终保留账户控制权

不是中心化交易所
    不撮合，不做市，不持仓
    不触碰用户资金池

不是跨链桥
    仅路由现有成熟桥（Stargate/LayerZero）
    不自建资金池，不运营桥节点

不是 DeFi 协议
    仅作为链上操作的抽象执行层
    本身不提供金融产品与收益服务
```

---

## 二、核心架构决策：MPC + Account Abstraction

### 2.1 为什么不做完全托管

```
完全托管 = 平台持有完整私钥
    本质是中心化钱包 / 交易所
    在中国属于严监管红线
    资金被盗、跑路、政策风险全算平台的
    这条路不能走
```

### 2.2 为什么不做纯非托管

```
纯非托管（MetaMask 模式）
    弹窗签名
    手动切换网络
    手动授权
    用户要管助记词

这和「像微信支付一样顺滑」完全相反
达不到产品目标
```

### 2.3 为什么只能是 MPC + AA

```
监管友好
    平台不持有完整私钥
    用户掌握密钥分片和控制权
    资产逻辑上属于用户自托管
    监管风险降一个等级

体验目标
    AA 原生支持
        无感知签名（Session Key）
        无 Gas（Paymaster 代付）
        批量交易（一次确认，多个操作）
        设备无关（社交恢复，换设备不丢账户）

Gas 垫费天然适配
    Gas Sponsor 是 AA 的原生能力
    Paymaster 合约直接支持稳定币代 Gas
    不用 AA，垫费逻辑要绕巨大弯路
    用 AA，开箱即用
```

### 2.4 安全模型（精准表述）

```
账户模式    Account Abstraction（ERC-4337）智能账户

私钥体系    MPC 分布式密钥管理
    用户持有客户端密钥分片
    平台持有服务端协同分片
    无任何一方拥有完整私钥

签名方式    MPC 多方协同签名
    客户端 + 服务端协同完成
    私钥永不拼接，永不暴露

助记词      用户可选择性备份（社交恢复 / 邮箱恢复）
            平台不持有，不存储助记词

一句话
    Axon 采用 MPC + Account Abstraction 非托管安全架构
    平台不持有完整私钥
    通过多方协同签名实现无感体验
    用户完全掌控资产所有权
```

---

## 三、三层架构

```
上层：Axon 生态产品
    Axon Pay（C 端 App）
    Axon Wallet（钱包模块，可嵌入第三方）
    极简 UI，负责用户交互

中层：Axon Protocol
    核心抽象层
    链路由决策器
    Gas 代付（Paymaster）
    MPC 签名协调
    统一接口暴露

底层：web3-blitz
    链交互引擎
    HD 钱包 / BIP32/44
    合约调用
    跨链桥接入
    已验证的充提流程
```

---

## 四、Gas 代付体系

### 4.1 Gas 金库管理

```
独立 Gas 垫资金库（多签托管钱包，非用户资金池）

监控规则
    实时监控各链 Gas 金库余额
    低于预警线    自动触发链下充值（人工 / 半自动）
    低于止损线    暂停该链垫付，用户走原生 Gas 自付模式

绝对禁止
    从用户资金池挪用垫付
    跨链自动拆借（风险不可控）
    资金混同（违反审计 / 合规）
```

### 4.2 Gas 波动风险分担

```
预估 Gas = 实时链上 Gas × 安全系数（1.2 ~ 1.5）
最终扣费    按实际消耗 Gas 折算稳定币扣用户

偏差分担规则
    偏差 ≤ 安全系数      用户承担（正常波动）
    偏差 > 安全系数      Axon 承担超额部分（极端拥堵）
    超过单笔 Gas 上限    拒绝垫付，通知用户手动处理

原则
    正常波动用户担
    极端拥堵平台担
    防止被恶意 Gas 战争吸血
```

### 4.3 稳定币 → Native Token 对冲

```
主方案：后台预储备 + Oracle 记账对冲（不走实时链上兑换）
    Axon 后台预先在各链备好 ETH/BNB
    用户支付稳定币
    系统内部按实时 Oracle 汇率记账对冲
    平台定期集中处理稳定币 ↔ Native 仓位再平衡

优点
    避免 DEX 滑点
    避免 MEV 攻击
    避免兑换延迟和失败

备用方案：链上兑换（仅在仓位再平衡时使用）
    Ethereum    Uniswap V3
    BSC         PancakeSwap
    强制：最小滑点保护 + 单笔限额 + 失败回滚
```

---

## 五、多链路由决策器

### 5.1 优先级策略

```
默认模式：速度优先
    选确认最快、出块最快、不拥堵的链
    适合：提现、急转、用户要秒到

用户可切换：成本优先
    选 Gas 最低、手续费最低的链
    适合：大额、不着急、用户在意成本
```

### 5.2 路由评分公式

```
路由得分 = 速度系数 × 0.6
         + 成本系数 × 0.3
         + 安全系数 × 0.1

实时输入
    速度系数    当前出块时间 + 交易池拥堵度
    成本系数    当前 Gas Price + 跨链手续费
    安全系数    近 24h 链上异常事件 + 桥成功率
```

### 5.3 跨链转账处理

**桥选择（白名单制，不动态乱选）**

```
主桥        LayerZero / Stargate
            统一接口，覆盖全链，成功率最高

备选        CBridge

禁止        未经安全审计的小桥，资金池不明的桥

产品文案
    「跨链统一通过 Stargate (LayerZero) 路由
     不使用未经安全审计的第三方桥」
```

**责任边界（写死，不可模糊）**

```
Axon 路由 / 配置错误导致失败
    → Axon 全额兜底，原路退回或重发

外部问题（桥故障、链拥堵、区块回滚、黑客事件）
    → 按桥方规则处理
    → Axon 协助追踪，提供交易证明，优先处理工单
    → 不兜底资金损失

用户填错地址 / 链
    → 用户自行承担
    → Axon 尽力协助追回但不承诺

标准文案
    「跨链基于第三方桥协议，因桥服务故障导致的异常
     平台协助追踪但不承担资金兜底责任；
     因平台路由配置错误导致的失败，由平台全额兜底并重新发起」
```

---

## 六、统一接口（开发者）

```http
# 查询余额
GET /api/v1/balance

# 充值（获取万能收款地址）
GET /api/v1/deposit

# 转账（无需选链，自动匹配最优路径）
POST /api/v1/transfer

# 稳定币兑换
POST /api/v1/swap
```

**屏蔽所有链差异，仅暴露 4 个核心接口。**

---

## 七、与 web3-blitz 的衔接

```
web3-blitz 已有（直接复用）
    HD 钱包（BIP32/44）
    充提币流程
    PostgreSQL 账户系统
    etcd 分布式锁
    JWT 身份验证
    Prometheus 监控

Axon Protocol 新增
    ERC-4337 智能账户合约
    MPC 签名协调服务
    Paymaster 合约（Gas 代付）
    多链路由决策引擎
    Gas 金库监控服务
    稳定币 Oracle 对冲模块
    跨链桥路由接入（Stargate/CBridge）
```

---

## 八、目录结构

```
axon/
├── cmd/
│   └── axon/
│       └── main.go          ← HTTP server 入口
├── internal/
│   ├── router/              ← 多链路由决策器
│   ├── paymaster/           ← Gas 代付逻辑
│   ├── mpc/                 ← MPC 签名协调
│   ├── bridge/              ← 跨链桥接入（Stargate/CBridge）
│   ├── oracle/              ← Gas 价格 Oracle
│   └── vault/               ← Gas 金库监控
├── contracts/               ← Solidity 合约
│   ├── AccountFactory.sol   ← ERC-4337 账户工厂
│   ├── Paymaster.sol        ← Gas 代付合约
│   └── EntryPoint.sol       ← AA 入口点（引用 EIP-4337 标准）
├── api/
│   └── v1/                  ← REST API 定义
├── Makefile
└── go.mod                   ← module: github.com/Ixecd/Axon
```

---

## 九、和 Feelings 生态的关系

```
Axon 是 Feelings 链上支付层的基础设施

FEEL 代币的支付
    用户购买感受包 → Axon 处理 FEEL 转账
    Gas 由 Axon Paymaster 代付
    用户只感知「付了多少 FEEL」

设备激活
    设备激活消耗 FEEL → Axon 处理链上注册
    激活流程对用户完全无感

创作者分成
    平台向创作者结算 FEEL → Axon 批量处理
    Transaction Batching 降低 Gas 成本

Axon 的定位
    不只是加密货币支付工具
    是 Feelings 生态所有链上操作的统一执行层
```

---

*把大象关进冰箱需要几步：打开门，放进去，关上门。Axon 负责让这三步对用户是透明的。*
