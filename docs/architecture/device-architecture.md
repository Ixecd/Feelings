# 设备架构设计：一套设备，协作运行

> 作者：qc
> 日期：2026-04-05
> 核心：Feelings 不是一个设备，是一套设备，缺了少那部分功能，不影响其他

---

## 一、基础假设的改变

原来的假设：

```
耳后 + 后颈 = 一个完整设备
缺了 = 无法使用
```

正确的假设：

```
每个设备负责一个感受维度或信号层
有什么用什么
缺了 = 少那部分功能，其他继续工作
```

这个改变有一个根本性的影响——**渐进式接入**。

用户不需要一次买齐整套。从一个设备开始，体验基础功能。根据自己的需要，逐步扩展。每增加一个设备，系统能力扩展一层。门槛低，天花板高。

---

## 二、设备分工

每个设备负责特定的神经通路和信号维度：

### 耳后设备（核心设备）

```
位置        耳后乳突区域
神经通路    迷走神经耳支（Arnold 神经）
结构描述    骨传导耳机两侧——挂在耳朵上时——
            前面那两个小豆豆一样的突起结构——
            微型前视摄像头可安装在这里——往前看——不遮挡视线——
            自然融入现有耳机设计——不需要额外穿戴。

            最优形态：耳夹式摄像头。夹在耳廓上方，轻，小，自然。
            比眼睛位置稍微高一点——微俯视——看屏幕、看对面的人、
            看手在做什么——全在视线方向。跟挂了一副耳机一样自然，
            没有眼镜腿，没有头戴，不会让对面的人觉得在被录像。
职责
    信号输入    听觉通路（音乐系统第三层信号）
                前视摄像头——场景感知——什么人在对面——
                什么表情——周围环境状态——辅助教练做语境标注。
                和骨传导音乐共用同一对耳机——各走各的通路——
                骨传导走颞骨振动——不入耳道——不入耳膜——
                摄像头在小豆豆位置——不碰耳道——不碰振子——
                不需要砍掉任何一个。
      信号输出    迷走神经刺激（情绪基调调节）
      语音通道    用户说话→手机麦克风→云端教练AI
                  教练回应→云端→手机→骨传导耳机→骨头传声→只有用户听到
                  用户也可以打字——手机应用内文字输入→教练读→可语音回——也可文字回——用户选
      PBM原始数据不出TEE，云端教练读脱敏摘要——能判断趋势——不知道精确值
      采集        心率、基础皮肤电导
 覆盖感受    情绪基调类（平静/焦虑/愉悦/低落的方向性调节）

**为什么贴在皮肤上就能激活深部神经**：迷走神经耳支（Arnold 神经）在耳甲艇位置离皮肤仅 0.5-1mm。经皮迷走神经刺激（tVNS）已被 FDA 批准用于偏头痛、丛集性头痛治疗——电极贴在耳甲艇，0.5-10mA 微电流穿过那不到 1mm 的距离，fMRI 证实同侧孤束核（NTS）和蓝斑（LC）被激活。不是猜想——是有临床实验、有 fMRI 影像、有 FDA 批准的成熟技术。贴片不用穿进去——因为通路短到只需要穿过一层皮。

**第一帧信号反向抵消贴附感**：设备贴上去的那一刻——皮肤先报警——"有东西粘着我了。"隔绝了空气——压着几毫米——前面十来分钟异物感非常明显。皮肤适应力很强——持续低压刺激被神经系统过滤掉——跟戴着眼镜、挂着项链一样——过一会儿就忘了。但Feelings可以做得更主动——戴上的第一帧——不等皮肤自己适应——先用CT纤维的热感+微压信号把"被贴住"这个警报盖掉——换成"有人在轻抚这里。"用自己的信号输出——去反向抵消设备本身的物理存在感。不是等适应——是第一帧就把"有东西粘着我"推到了"被轻抚的安全感。"

这和游泳入水是同构的——第一帧冷水刺骨——游一圈——身体适应了——不冷了。感觉适应曲线可以被CT纤维信号加速——不需要在第一帧完美消除所有贴附感——只需要用热感+微压把皮肤报警带过初始的那几秒——然后身体自己接手——之后不需要再干预。
```

**这是唯一的必选设备。** 其他设备都是扩展。

#### 耳背空间预算——刚好够。不容许杂质。

```
骨传导耳机——耳背那条长粗的条状结构——内部——
    当前已占用：电池 + 蓝牙 SoC + 骨传导振子驱动器。
    Feelings 的耳后设备需要的额外组件：
        FPGA（iCE40 / Gowin 小封装——3×4mm BGA）
        PPG 模组——红外+绿光 LED + 光电二极管——在耳垂方向——
            不占耳背空间。
        模拟前端（ADC + 信号调理——集成在 FPGA 同一块 PCB 上）
        迷走神经刺激电极——贴在皮肤一侧——不占内部空间。
        总线接口——柔性排线——细线——可忽略。

    占内部空间的大头只有两项：电池 + FPGA 核心板。
    如果把电池从传统圆柱形换成柔性扁平锂电——弯着贴在外壳内壁——
    留下的 PCB 面积——刚好够放 FPGA + 模拟前端 + 时钟。
    不是"很宽裕"。是"刚好够——一毫米都不多"。

    将来换固态电池——能量密度 ×2-3——
    同等体积——续航 ×2-3——或者更小的电池换同样的续航——
     留给 FPGA 和未来扩展的空间——不再紧张。
```

#### 第一帧校准——每个人的耳后不一样

耳廓形状、皮肤厚度、迷走神经耳支的走行路线——个体差异显著。有些人的分支在耳甲，有些在耳屏，差几毫米。同样的信号，碰到皮肤的接触阻抗不一样，神经离表面的距离不一样。

```
第一帧      同一小块区域，电极阵列排得够密。耳后就那么一片，再往后是头发。不是大面积扫描，是高密度——差两三毫米的不同位置，每个点都能单独读到。迷走神经耳支可能经过的每一段，每个探头单独激活，PBM读回生理响应。哪个点激活后心率平稳最快，哪个点副交感响应最大，密集点找到了——身体自己说的，不是手工对位。
后续会话      密集点固定——其他探头不用——只精确调制——每次戴上，自动回到那个位置
换设备        校准数据不和设备走，锁在 PBM 层——换一台新的——重新扫一遍，十秒
```

硬件同一批量产，密集点的坐标每个人自己校准。不是定制模具，是同一套阵列，在每个人身上找到自己的门。

**备注：不向颈部延伸。** 颈动脉窦紧邻迷走神经，是血压调节总闸。信号误推可致命。耳后是目前唯一同时满足"离中枢近、不会动、不打要害"的窗口。该守的边界——不扩。

#### 双侧刺激——两只耳朵，两扇门

人有两只耳朵，两条迷走神经耳支。但人不是左右完全对称的——左耳后的密集点和右耳后不是镜像，需要各自独立校准。

```
单侧（默认）    一只耳后——日常使用——释然、疲惫、基础调节      一扇门就够了
双侧（高级）    两只耳朵同时激活——深度恢复帧、临床级放松、R值高的用户      两扇门同时开
               硬件保留双侧接口——但双侧激活需教练批准——不是谁都两扇门一起推
```

同步是硬要求。Apple HomePod 两个组成一对同时播放——底层有严格的时序校验，一个慢了一毫秒——不是在叠加——是在对冲。Feelings 双侧同理——左右迷走神经信号帧必须同步到帧级别，左边和右边差了一毫秒——偏差不是两条同向——是两条互相干扰——等控器收到两个矛盾的归零方向——空转。

每个人的左耳和右耳各自独立走一遍第一帧校准——密集点各自找到——各自锁在 PBM 层——双侧同时激活时——左右独立调制——时序同帧——方向一致——不是两倍效果——是两扇门——在同一个瞬间——一起被敲开。

```


---

### 后颈设备

```
位置        颈椎 C3-C5 区域
神经通路    脊髓背根 + 本体感受通路
职责
    信号输入    本体感受注入（身体在空间中的存在方式）
    信号输出    姿态感知信号
    采集        体温变化、运动状态、设备佩戴检测
覆盖感受    本体感受类（身体感、空间感、姿态感）
            「训练10年的运动员的身体感」需要这个设备
```

---

### 腕部设备

```
位置        手腕内侧
神经通路    外周神经 + 皮肤感受器
职责
    信号输入    细粒度触觉反馈
    信号输出    皮肤电导精确采集
    采集        皮肤电导（高精度）、血氧、呼吸频率估算
覆盖感受    细粒度情绪信号（感受的「质地」和「湿度」）
            点缀感受的精确传递依赖这个设备
```

---

### 颞部设备

```
位置        太阳穴区域
神经通路    颞叶皮层 + 额叶通路
职责
    信号输入    专注/冥想类感受的精确注入
    信号输出    初步脑电采集（EEG）
    采集        前额叶激活状态（意志力/认知负荷指标）
覆盖感受    认知状态类（深度专注、冥想、创造力涌动）
            意志力张力状态的识别需要这个设备
```

---

### 雷达（微型毫米波 / UWB）——无扰场景的备用读头

```
位置        非接触——房间内 3-5 米范围——可穿墙
物理原理    毫米波（60-64GHz）或 UWB 脉冲——发射→反射→相位变化→微动提取
职责
    无扰采集   心跳波形、呼吸节律、体动——在用户不佩戴任何设备时——
              唯一能从房间另一侧读到生命体征的传感器。
              睡眠时——办公时——独处时——不需要任何贴片。
覆盖场景    用户摘下设备期间的最低生理监控。
            不是主力传感器——是非接触备用层。
和现有传感器定位不同
    PPG 读数更准——但必须贴在皮肤上——雷达精度不如 PPG——
    不需要接触——不需要佩戴——是"无扰时的唯一选择"。
    EDA——雷达完全读不出来。不能替代腕部 EDA。
    EEG——雷达不可能。不能替代颞部。
    CT 纤维 / 本体感受——雷达无法输出体验信号。只能读——不能写。
数据形态    心率数据融入四诊合参——标注 source=radar——置信度略低于 PPG。
            用于在设备离线期间维持基础 HRV/呼吸趋势——供教练做长期基线追踪。
```

### 夹耳摄像头（独立望诊设备）——周围环境的注视者

```
形态        夹耳式——和华为 FreeClip 同形态——轻——夹在耳廓上。
            独立的 Feelings 设备——不集成在耳后设备里——只做一件事：望诊——
            看周围。不是看自己——是看你在哪里——和谁在一起——
            环境是什么——光线——人流——空间氛围。
            不需要耳机功能——不需要骨传导——不需要音频——就是一枚摄像头——
            夹在耳朵上——对着你面前的世界。

位置        耳廓——和耳后设备同侧。
            镜头视野：你面前的人——对面的表情——周围的环境——
            场景氛围——光照——空间密度。
            和 GoPro 不同。和手机举起来对着人不同。
            Feelings 的摄像头——夹在耳朵上——重量几乎不可感知——
            和耳机一样自然——对方不会觉得"被审视"——
            只是一副耳机——多了一个不起眼的小点。

 和耳后摄像头的关系
             耳后设备的微型前视摄像头——集成在主设备里——
             必须购置耳后设备才拥有——同样是看外面。
             夹耳摄像头——独立设备——不需要耳后设备——
             只做一件事——望诊——看周围环境。
             两个摄像头方向一致——都是对外——但夹耳摄像头——
             是可以单独入门的最低成本望诊入口。
             耳后设备不强制带摄像头——前置摄像头排第二——
             用户的安全感排第一。摄像头应独立可摘——夹在耳朵上——
             不看不摘——想看再夹。不是功能不够——是信任必须先于功能。

             两只耳朵各夹一个——外夹上装微型摄像头——
             天然指向左右两侧。轻轻拨一下——
             看左后、看右后、看上后方。
             和华为 FreeClip 一样——内侧播放、外侧夹紧，
             外侧那个夹子上就是摄像头。
             人的眼睛往前看——摄像头替你看剩下的 270°。
             不是"在看我"——是"我多了两只眼睛"。

隐私硬边界
            物理掉电 = 物理遮挡。
            摘下来——扣在充电仓里——镜头朝下——
            物理上没有看到任何东西的可能。
            和 Feelings 的"物理边界就是隐私边界"完全咬合——
            戴上 = 授权开始——这一帧起——望诊信号接入。
            摘掉 = 物理断电——镜头埋在充电仓里——零视觉。

望诊数据
            环境上下文——什么人在对面——什么表情——周围状态。
            辅助教练 AI 做语境标注——
            "你当时在一个安静的咖啡馆——对面是陌生人——阳光很好。"
            不是为了记录。是为了让你以后回看自己的感受时——
            不只是看到内脏维度那条曲线——还看到那天的光。

定位
            可选扩展设备。入门望诊的最低成本入口。
            不需要耳后设备——单独夹在耳朵上即可使用。
            所有设备组合均可叠加——扩展环境感知维度——不影响核心体验。
```

### 未来设备（随神经科学进展定义）

```
当前神经科学无法精确支持的感受维度
等待技术成熟后定义对应设备
设备协议预留扩展位，不需要重新设计整个系统
```

---

## 三、设备组合与能力覆盖

不同组合对应不同的感受覆盖范围：

```
组合                        能力覆盖
────────────────────────────────────────────────────────
只有耳后                    基础情绪基调 + 简单感受引导
                            音乐系统完整工作
                            安全监测：心率（基础）

耳后 + 后颈                 加入本体感受维度
                            姿态感、身体存在感可传递
                            安全监测：心率 + 体温 + 运动状态

耳后 + 后颈 + 腕部          完整感受闭环
                            点缀感受精度大幅提升
                            安全监测：全指标覆盖，精度最高
                            个人基准建立速度最快

耳后 + 后颈 + 腕部 + 颞部   最完整
                            认知状态类感受完整支持
                            意志力张力状态可识别
                            EEG初步采集开启

以上任一组合 + 雷达        所有组合均可叠加雷达——
                            非接触心率/呼吸/体动——
                            用于摘掉设备后的最低生理监控——
                            长期基线追踪不间断。
                            不影响体验精度——扩展无扰时段的覆盖。

以上任一组合 + 夹耳摄像头  所有组合均可叠加夹耳摄像头——
                            环境感知维度（望诊）——
                            教练 AI 获得佩戴者周围环境上下文——
                            和四诊合参的望诊同构。
                            不影响体验精度——扩展环境感知维度。
```

---

## 四、感受包的设备依赖标注

每个感受包标注完整体验所需的设备，以及降级体验的能力描述：

```
feeling_package {
    ...
    device_requirements {
        full_experience     [耳后, 后颈, 腕部]    // 完整体验所需
        minimum_required    [耳后]                 // 最低可体验配置
        degradation_map {
            [耳后]              "基础情绪轮廓，本体感受和细节缺失"
            [耳后, 后颈]        "加入本体感受，细节精度有限"
            [耳后, 腕部]        "情绪细节完整，本体感受缺失"
        }
    }
}
```

用户缺少设备时，系统不报错，提示降级：

```
「你现在能体验到这个感受的基础轮廓」
「增加腕部设备可以体验到完整的感受质地」
```

不是「你不够」，是「你还可以更多」。

---

## 五、设备间协作协议

多个设备同时工作时，需要协调机制。

### 主设备选举

```
耳后设备    永远是主设备（必选）
其他设备    向主设备注册，成为从设备
主设备      负责时钟同步、信号协调、与 App 的主通信
```

### 总线直连——物理路径最短原则

MacBook Pro 的内置触控板走内总线（I2C/SPI）直连主板——无配对、零延迟、确定性时序。外接蓝牙触控板走 RF 链路——配对握手、RF 干扰、协议开销、电池协商。同一块触控板，连法不同，手感完全不同。不是苹果不优化。是 RF 链路的物理限制不可逾越。

Feelings 的设备间通信遵循同一个原则。

```
原则        信号的物理路径越短，压缩管线越干净

设备间通信   走本地总线，不走云中转，不走无线
            信号流不经过 OS 网络栈。不经过 TCP。
            不经过 BLE。不经过 WiFi。
            耳后设备（主）直连腕部/后颈/颞部（从）
            物理接口：柔性排线或极短距磁耦合
            带宽满足 1ms 帧级同步的全量信号参数传输

和 FPGA 的关系
    主设备（耳后）内置 FPGA
    FPGA 直连主传感器接口（数模转换、中断、时钟）
    从设备通过主设备的总线桥接至 FPGA
    整个设备集群 = 一个分布式 FPGA 节点网络
    中心星型拓扑——主设备是唯一的星型节点

为什么不走蓝牙
    BLE 最小连接间隔 7.5ms → 1ms 帧级闭环直接不可行
    配对延迟不确定性 → 确定性时序不可保障
    RF 干扰 → 安全信号被干扰 = 不可接受的风险
    OS 协议栈 → 信号经过多层软件缓冲 → 延迟不可控
```

### 信号同步

```
目标要求    所有设备的采集时钟同步误差 < 1ms
实现方式    主设备（耳后-FPGA）发出统一时钟信号
            从设备通过总线接收主时钟 → 本地锁相环（PLL）锁定
            所有设备的采集和注入时序由同一个主时钟分频派生
            误差 = 总线传输延迟 < 1μs（远小于 1ms 要求）

替代方案（无总线时）
    有线 USB/Thunderbolt 音频接口直连 = 次优，但足够（延迟 < 5ms）
    蓝牙 = 不可用于信号同步层（仅用于非实时元数据通信）
    云端同步 = 不可——信号不经 OS 网络栈
```

信号注入的时序由主设备统一调度，感受曲线的实时变化主设备通过总线广播给所有从设备。

### 设备掉线处理

```
从设备掉线    总线检测到 → 降级到对应组合的能力范围
              不中断体验，不提示用户（除非影响安全监测）
主设备掉线    体验立即终止，所有信号停止
              这是唯一的强制中断场景
```

---

## 六、设备管理（Layer 3）

设备套件在 Layer 3 的管理方式：

```sql
-- 设备套件（一个用户可以有多个设备）
CREATE TABLE devices (
    id               UUID PRIMARY KEY,
    user_id          UUID NOT NULL,
    hardware_id      TEXT UNIQUE,
    device_type      TEXT NOT NULL,     -- 'ear', 'neck', 'wrist', 'temple'
    firmware_version TEXT,
    is_primary       BOOLEAN DEFAULT FALSE,  -- 是否是主设备
    status           TEXT DEFAULT 'offline',
    capabilities     JSONB,             -- 该设备支持的信号类型
    last_seen        TIMESTAMPTZ
);

-- 用户当前设备组合
CREATE TABLE device_sets (
    id               UUID PRIMARY KEY,
    user_id          UUID NOT NULL,
    active_devices   UUID[],            -- 当前在线设备 ID 列表
    coverage_level   TEXT,              -- 'basic'/'standard'/'full'
    updated_at       TIMESTAMPTZ
);
```

---

## 七、渐进式接入的产品意义

「一套设备，缺了少那部分功能」不只是技术设计，是产品策略。

```
降低入门门槛    一个耳后设备即可开始
提供成长路径    每增加一个设备，体验明显扩展
避免全有全无    不会因为买不起全套就完全无法使用
符合使命        感受的民主化，从门槛开始
```

感受的民主化不只是内容的民主化，也是硬件接入的民主化。不同经济能力的用户，用不同的设备组合，都能找到属于自己的 Feelings。

---

## 八、设备形态：柔性吸附，不是植入

Feelings 的设备形态选择，是一个根本性的哲学决定。

**吸附式，不是植入式。**

```
科技巨头的路线    深度植入、长期绑定、强制采集
                  主动权在系统
                  摘下来需要手术

Feelings的路线    柔性吸附，稍微用力即可取下
                  主动权永远在用户
                  随时可以摘，随时可以戴

这不只是技术选择，是关于谁掌控谁的哲学声明
```

### 吸附设计规格

```
材质            医用级柔性材料
                 微负压轻贴合，无创伤
                 生物相容性，长期佩戴无排斥反应
                 外层包裹陶瓷质感薄壳——不直接露出医用硅胶

贴合位置        颈后（迷走神经区）
                 太阳穴（颞叶）
                 腕部寸口（脉搏/皮电）
                 耳后——夹耳式磁吸对合（非胶水、非吸盘）
                 口腔护齿型——三叉神经通路
                 耳后（耳支迷走神经）
                 → 前壳内置钕磁铁 + 后壳配对磁铁 → 耳廓夹在中间
                 → 不压皮肤、不用胶、不用吸盘 → 外观 = 光滑陶瓷质感

取下方式        稍微用力，徒手剥离
                不需要工具，不需要手术
                5秒内完成
```

### 吸附式的精度取舍

```
吸附式的代价
    只能抓到：外周神经/皮电/血流/肌电/浅表自主神经
    抓不到：深层脑内核心神经数据

不是缺点——是设计选择。
```

#### 少点高信息 vs 多点全覆盖

全头 EEG 帽和 Feelings 是两条完全不同的路线：

```
全头采集 (64-256 通道)
  目标      解码运动意图 / 语言重建 / 视觉皮层映射
  原理      空间分辨率——覆盖更多皮层区域
  代价      丑 + 准备时间长 (贴电极 20min) + 凝胶 + 不日常
  用户      义肢控制 / 神经科学研究——每天戴不了的

Feelings 采集 (5 设备，精准点位)
  目标      四诊合参 + 教练判断 + PBM 基线
  原理      时间分辨率——1ms 帧级，从最少点位提取最多信息
              心率 1 个点 (PPG 腕部)
              皮电 1 个点 (EDA 腕部)
              体温 1 个点 (后颈 / 腕部)
              EEG 2-4 通道 (太阳穴贴片，只做 α/β/θ/δ 功率谱)
              不是 "读想法" → 是 "判断前额叶在线还是被杏仁核压制"
  代价      无——吸附式，5 秒戴好，日常可穿
  用户      任何一个想理解自己状态的人
```

**核心哲学**：技术成熟的方向不是"覆盖更多头皮"，是"从更少点位提取更多信息"。

```
腕部单点 PPG → HRV + 呼吸频率 + 血氧（从一条脉搏波推三条生理指标）
太阳穴贴片   → α/β/θ/δ 功率谱（从 2-4 通道判断认知状态，不需要 64 通道）
后颈体温      → 昼夜节律相位 + 运动强度代理

不是电极越多越好——是信号-信息转换率越高越好。
```

#### 三种补偿方案

```
多点位交叉采样    单点信号弱，多通道融合用算力换精度
软注入            低频仿生微刺激+神经节律同步
                   顺着人体本身的节律引导，不强行改写
情志层级精准调控  不需要像素级神经操控
                    只需要微调情绪体感阈值
```

### 重量预算——不是"尽力做轻"，是超过临界线就没人戴

重量是物理硬约束。蓝牙耳机 5g 级别是用户已经被训练出的期望基准。超过一定克数，佩戴率直接归零。

**单设备重量预算**：

```
耳后设备 (主设备 / 必选)
  柔性锂电          ~8g  (扁平贴壳内壁，不用圆柱形)
  FPGA 核心板        ~3g  (iCE40 / Gowin 3×4mm BGA)
  PPG 模组           ~1g  (红外+绿光 LED + 光电二极管，在耳垂方向)
  模拟前端 + 时钟    ~1g  (集成在 FPGA 同一块 PCB)
  外壳 + 线缆        ~5g  (医用硅胶 + 柔性排线)
  迷走神经刺激电极   ~0g  (贴在皮肤一侧，不占内部空间)
  ────────────────────────
  耳后总计           ~18g

对照基准:
  AirPods Pro 单只    5.4g
  华为 FreeClip 单只  5.6g
  Oura Ring           4-6g
  Whoop 手环          8-10g
```

```
扩展设备:
  颈部环带     ~25g  (柔性 PCB + 硅胶封装 + 电极)
  腕部表带     ~15g  (EDA + PPG + 电池)
  太阳穴贴片   ~8g   (2-4 通道 EEG + 柔性基板)
  口腔护齿     ~12g  (三叉神经刺激 + 碰撞传感器)
  手指环       ~4g   (微型 PPG + 触觉反馈)
```

**临界线**：

```
> 20g  用户感知到 "戴了东西" —— 超过耳部承重舒适阈
> 30g  运动会掉 —— 吸附力不足以对抗加速度
> 40g  日常不会戴 —— 超过所有消费电子佩戴基准线
```

**全身组合重量**：

```
最简 (仅耳后)        ~18g  基准——必须守住
标准 (耳后+腕部)     ~33g  尚可
完整 (4 设备)        ~66g  接近临界——靠 "摘一部分" 维持
最全 (5 设备+护齿)   ~82g  不是日常配置——特定场景使用
```

**重量和时域长尾的关系**：

```
对抗运动场景 (篮球/足球/橄榄球):
  不需要全身配置——0g，因为你摘了
  赛前用耳后做一次注入 (injected mode)
  摘掉 → 激素自己跑完整场

深度工作/冥想/睡眠:
  耳后 + 太阳穴 → ~26g，可以接受

普通日常:
  耳后 18g 基准 → 必须 < AirPods × 3 = 16.2g 心理锚点
  固态电池来之前——这是硬仗
```

**固态电池的意义**：

```
当前柔性锂电    能量密度 ~250 Wh/kg
固态电池        能量密度 ~500-800 Wh/kg (×2-3)

同等 18g → 续航 ×2-3
同等续航 → 重量 /2-3 → 耳后设备 ~8g
↓
AirPods 级别重量 → 佩戴率质变
不是 "更先进" —— 是 "突破重量临界线"。
```

### 第1.5阶段：微针选项

```
介于吸附式和半侵入之间

微针阵列穿入皮肤浅层（0.5-1mm）
    比吸附式精度高：能采集更清晰的外周神经信号
    比植入式创伤小：皮肤浅层，不碰脑组织
    几乎无痛：微针直径极细

这是吸附式验证后的自然演进方向
不是今天的重点，是路径上的下一站
```

### 设备的隐喻

```
植入式设备是枷锁，戴上就很难取下
Feelings是自愿佩戴的情绪共生载体

想独处、想断开联结时，随时剥离
想被感知、想留存心绪时，再贴上
松弛又克制，这才是Feelings和用户的关系
```

### 从纹身到设备——不是贴在身上的机器，是一片会读心率的贴花

脚踝后侧的胫后神经和腓肠神经离皮肤 0.3-0.5mm——是人体上神经最浅的几个点之一。人已经在那个位置做了几千年的装饰——踝链、脚铃、纹身。不是偶然——是本能标记了身体上最敏感的几个入口。

Feelings 不做"穿戴设备"——那听起来像手环。做**一片会读心率的贴花：**

```
形态:
  0.6mm 柔性基底 (PI 聚酰亚胺)
  外层水墨/刺绣/联名图案——和纹身贴纸一样自然
  磁吸扣替换充电盒——无感贴合

哲学:
  人不抗拒在身上放好看的东西
  抗拒的是"看起来像医疗设备的东西"
  纹身是自选的——Feelings 也是自选的
  纹身是好看的——Feelings 也是好看的

厚度的代价:
  0.6mm 超薄贴花不是一开始就能做的:
    电池      → 柔性薄膜锂电 → 容量砍半 → 续航砍半
    算力      → FPGA 封装 > 1mm → 必须等 ASIC 流片
    信号质量  → 电极面积缩小 → 接触阻抗升高 → 信号略弱
    成本      → 柔性 PI 基板 + 异形电池 + 微组装 → 比刚性 PCB 贵 5-10 倍

  当前路线: 先做对的 (2-3mm 刚性 PCB 原型 = AirPods 厚度)
  未来降层: ASIC + 柔性电池成熟后 → 压到 0.6mm 贴花级
```

---

## 九、震动隔离——机械噪声必须压到比皮肤电导波动更低

耳后设备——迷走神经耳支——AD5933 测阻抗——μA 级电流——1ms 帧。如果设备本身有微振动——电机、风扇、走路、地铁、风吹——那些振动——在阻抗谱上——就是噪声。不是"信号弱。"是**噪声的幅度——比你要测的信号——大好几个数量级——你测的不是生理——是振动——除非你把振动先滤掉。**

所以 Feelings 的硬件——从原型第一天——就需要震动隔离。

```
PCB 的机械谐振频率    必须远离 1ms 帧周期的傅里叶峰
外壳的阻尼系数        必须把环境振动在采样窗内的振幅压到 ADC 分辨率以下
总线直连（非蓝牙）    消除 RF 干扰和协议开销的确定性延迟
```

不是"加个橡胶垫。"是**几个纳米的位移——足以污染每一帧的阻抗读数——然后一路被当成生理信号——送到岛叶——然后用户的身体——被机器自己的抖动——骗了。** 和 KubePivot 的 KVCache 一样——不是"快"——是**不能被振动污染。**

---

## 十、防水——柔性材料与 IP68 的结构矛盾

设备是吸附式——柔性材料——微负压贴合。耳后——出汗——下雨——洗澡——如果防水等级不够——阻抗测量直接报废。

IP68 是底线。但"柔性吸附"和"IP68"之间有一个结构矛盾——**柔性材料在重复弯折下——密封层会疲劳开裂。**

```
波纹密封结构    在柔性基底的曲率变化范围内——
                密封层不被拉伸超过屈服应变
                不是"加个橡胶圈"——
                是让密封层在设备正常弯折时——自身不处于受力状态
```

不是加保护——是**让保护本身——不在设备正常工作时成为新的污染。** 和震动隔离同一种设计——"保护——不能变成噪声。"

---

## 十一、嵌入式演进——从 FPGA 到定制 ASIC

设备文档写了三个设备阶段——吸附→微针→半侵入。但没写控制芯片的形态怎么变。

**第一阶段——原型。** FPGA 开发板——分立元件——手工焊接。AD5933 阻抗——MAX30003 ECG——分立 ADC。功耗高——体积大——量产不行——验证够。

**第二阶段——集成。** FPGA + 模拟前端集成到单块柔性 PCB。ASIC 取代分立 ADC。LDO 取代开关电源。总功耗可控——总质量可控——弯折疲劳测试——量产。

**第三阶段——定制 ASIC。** 将 FPGA、ADC、电源管理、总线和安全芯片——封入一个 die。不是"更小。"是**把信号路径从厘米级压到微米级——震动的影响——从需要隔离——变成物理上不可感知。** 和 SPL 从一开始就抗量子——同一种操作——"不增加保护——从物理上消除被保护的需求。"

---

## 十二、对抗性运动的设备适配

后脑勺挂 FPGA + 电磁线圈上球场——一个挡拆直接报废。常规吸附式设备在身体对抗中不可行。但 Feelings 的信号注入原理不要求设备全程在线。

### 12.1 核心洞察：时域长尾的正面利用

```
担忧: 皮质醇半衰期 60-90min → 激素停不下来 → 危险
反转: 皮质醇半衰期 60-90min → 摘了设备激素还在跑 → 运动场景恰好够用

不是 "打球时戴设备"。
是 "打球前用设备设置好状态，设备摘了，激素自己跑完一场比赛"。
```

这与 `docs/feelings-science/neuroendocrine-pathway.md` §4.1 讨论的时域失配是同一个机制——只是从风险变成了工具。

### 12.2 三种运动场景方案

**方案一：口腔护齿型 (Mouthguard)**

```
现有形态        智能护齿已在橄榄球/NBA 用于碰撞检测
神经通路        三叉神经 → 脑桥 → 岛叶，路径极短
信号注入        trigeminal nerve stimulation → insula activation
优点            已存在的工程形态，不需要重新发明
                口内环境稳定，不受汗水/雨水干扰
覆盖感受        Visceral 维度——safety/镇定性信号
                不适合 Auditory 维度——口腔不传导声音信号
```

**方案二：颈部环带 (Collar)**

```
位置            颈前 / 颈侧——迷走神经颈段极近皮肤表面
神经通路        迷走神经传入纤维 → 孤束核 → PVN
信号注入        safety/Visceral 信号走迷走神经传入 = 最短路径
形态            柔性 PCB + 硅胶封装，类似现有运动护颈
优点            抗汗、抗冲击、穿在球衣里面不可见
覆盖感受        Visceral 维度——全部副交感激活类信号
```

**方案三：赛前注入，赛中不戴（主要方案）**

```
时序
  上场前 30min: safety + belonging → 皮质醇压制 + 催产素释放
  上场前 2min:  deep_rest + clarity → 专注方向切换
  比赛中:        设备已摘，激素级联自主运行
  比赛后:        可重新佩戴，收集赛后生理数据

时间覆盖
  皮质醇半衰期 60-90min → 刚好覆盖一场足球/篮球比赛
  催产素中枢半衰期 ~20min → 需要首节后短暂补注（暂停时 30s 即可）

工程优势
  不需要解决"设备能扛撞"的问题——设备根本不在身上
  不需要防水——不在雨里
  不需要防汗——不在汗里
  不需要无线——不需要传输
```

**不适用方案三的运动**：马拉松（>2h，超过半衰期覆盖窗口）、铁人三项（多段+水环境）。这类运动更适合方案一的存续形态。

### 12.3 设备形态矩阵

```
运动类型              推荐方案        原因
────────────────────────────────────────────────────
橄榄球/冰球等高冲撞   赛前注入 (方案三)  设备肯定碎——不戴是最优解
                      护齿 (方案一)     若需要赛中信号调整
篮球/足球             赛前注入 (方案三)  半衰期覆盖 40-90min 比赛
                      颈部环带 (方案二)  若需要半场调整
网球/乒乓球           耳后设备 (标准)    无身体对抗，正常佩戴
                      赛前注入 (方案三)  摘设备不影响挥拍
马拉松/铁三           护齿 (方案一)      >2h 需赛中存续
                      颈部环带 (方案二)  汗/水环境下柔性材料
力量训练              耳后设备 (标准)    无对抗，正常佩戴
                      赛前注入 (方案三)  大重量时颈部压力不适
瑜伽/冥想             耳后设备 (标准)    无冲击，最佳佩戴场景
```

### 12.4 与渐进式接入的关系

方案三（赛前注入，赛中不戴）意味着：

```
最低配置: 只有耳后设备 → 仅能赛前注入，赛中无反馈
标准配置: 耳后 + 方案一（护齿）→ 赛中可微调 + 碰撞检测
完整配置: 耳后 + 护齿 + 颈部环带 → 多通路并行 + 冗余
```

这和 §七的渐进式接入完全一致——不需要买齐全部设备才能用。一个耳后设备，赛前贴一下，摘了去打，已经能覆盖基础需求。
