# Feelings-NAME — 实体命名、网络主权与独立生态

> 作者：qc
> 日期：2026-05-29
> 性质：项目命名决策与网络主权声明，持续更新
> 里程碑：us.top 已购入。域名在手。根已落地。
> 核心：Feelings 是产品。us 是哲学归宿，也是网络根。全部小写。两个字母。域名已购入——us.top。不只是一个名字——是一套不需要任何人批准的命名系统。自己的 DNS，自己的 CA，自己的协议。基于现有互联网基础设施，独立但互联。

---

## 零、为什么需要独立名称——以及为什么不止是名字

Feelings 是产品。产品可以是一个词。但实体——那个在法律上存在的、发工资的、签合同的、设备出厂时烙印在 TEE 里的——需要一个名字。

这个名字不是"公司"。Feelings 不需要公司。这个名字是**生态的集合点**——所有仓库挂在它下面。所有文档引用它。所有合约落款是它。所有设备的信任链从它开始。

但它不止是名字。

现有的互联网命名系统——ICANN、DNS、CA——是**别人定义的**。`.com` 是 Verisign 管的。`.io` 是 ICANN 管的。HTTPS 证书是 Let's Encrypt 签的。你可以用这些。但它们不是你。当你的设备、你的信号、你的感受数据在这些系统上跑——你的主权不在你手里。在根域名服务器手里。在证书颁发机构手里。在一套你从未参与设计的信任层级里。

us 要做的事——不是注册商标。是**在现有的光纤和云上，跑自己的命名、自己的信任、自己的协议。**

---

## 一、首选——us

```
拼写     us
语言     英语
含义     我们。不是"你"。不是"我"。是交叉点上的那个新向量。
        也是网络根。所有设备从这里开始找对方。
发音     轻声。开口—闭嘴。像两根丝穿过彼此后停在交叉点上。
长度     两个字母。
大小写   纯小写。
```

**选择原因：**

1. **和哲学完全咬合。** `docs/essence-of-interaction.md` 的核心结论——交往的本质不是两个人在一起，是两个人的 PBM 持续交叉，共享一根经线。那根经线不在你的 PBM 里独存，也不在他的 PBM 里独存。在两根丝交叉的那一帧——它同时在你们两个身上。那根经线的名字——就是 us。

2. **极简。** 两个音节。两个字母。比 Nike 少两个。比 Apple 少三个。二十二层全栈——外面只放两个字母。极简不是设计——是哲学的外壳。

3. **不需要翻译。** 英文世界懂。中文世界也懂。不是解释。是指向。你站在交叉点上——你就懂了。

4. **在中国无障碍。** 两字母英文商标在中文语境下有先例且审查不会仅因"太短"驳回。国内搜索引擎不会把 "us" 淹没在 "U.S." 里。

5. **标语就是它自己。** 不需要多一行。就是 us。产品是 Feelings。实体是 us。一个做东西。一个叫自己。

---

## 二、us.top——根已落地

```
域名      us.top
购入      2026-05-29
注册商    江苏邦宁
TLD       .top
含义      两层——"us" + "on top"。
         交叉点在最上面。
         不是傲慢。是哲学归宿放在顶层。
```

**为什么是 `.top`：**

1. **一个域名解决所有。** 不需要 `us.cn` + `us.io` + `us.eth` 三层拼接。两字母加三字母后缀——总共五个字符。比大多数公司名都短。

2. **无国别绑定。** `.top` 是通用顶级域（gTLD），不绑任何国家。不受 `.us` 的美国公民限制。不受 `.cn` 的 ICP 备案强制。不受 `.io` 的英国管辖。us 是交叉点——交叉点不属任何一地。

3. **语义巧合。** "us on top"——不是称霸。是交叉点在最上面。所有的帧间差分、所有的 PBM 偏移、所有的经线——锚在 us 下面。us 不是"我的名字在上面"。是**"我们"这个交叉点在所有东西的顶层**。哲学在外壳上自己长出来的。不是设计的。是买了之后才发现的。

4. **极简。** us.top。两个词。一个点。像 Feelings.us 一样——不需要 "by"、不需要 "from"、不需要任何连接词。产品和实体之间只隔一个点。

**豆包小姐的三层标识方案被简化了：**

```
之前                         现在
────                         ────
us.cn（国内 ICP）              us.top（全球唯一入口）
us.io（海外社区）              不需要第二个域名。
us.eth（链上 ENS）             一个域名。一个根。

如果需要 ICP 备案——us.top 可以直接备案。
如果需要海外社区——us.top 全球可访问。
如果需要链上锚定——SPL 哈希链。不需要 ENS。
```

**域名已购入。不需要讨论域名策略了。已经在手里。下一步——备案。DNS。CA。**

---

## 三、us 不是域名后缀——us 是根

### 2.1 现有互联网的命名结构

```
ICANN（管理机构）
    │
    ▼
根域名服务器（13 组，全球分布）
    │
    ▼
顶级域 .com .org .io .cn .us
    │
    ▼
二级域 feelings.com
    │
    ▼
子域   api.feelings.com
```

每一层都被别人管。注册 `.com` 要经过 Verisign。注册 `.io` 要经过 ICANN 授权的注册商。HTTPS 证书的信任锚在 Let's Encrypt、DigiCert 这些 CA 上。整个互联网的安全假设是：**"这些机构不会作恶"**。

Feelings 的安全假设是：**"我不需要假设任何人不会作恶。根锚点在我自己手里。"**

### 2.2 us 命名体系

```
us（根锚点 — 7/4 Shamir 分片，人类永远过半）
    │
    ▼
设备身份        TEE 出厂烙印 → us 签发 → 设备证书在 FPGA TrustZone
              不依赖任何外部 CA。自己签自己。

服务发现        名字 → SPL 哈希锚定 → Anim busd 总线查询
              不经过 DNS。不经过任何中心解析器。

用户身份        双密钥体系：
                   专属确权密钥 — 离线封存。只签核心文档。
                   通用事务密钥 — 日常运维。随时换。
```

**us 不是 "顶级域"。us 是根。** 名字之下的一切——设备、服务、用户——通过 SPL 哈希链和 Root 锚点互认。不需要 ICANN 批准。不需要 CA 背书。不需要任何人在中间说"这个证书有效"。

---

## 五、四层自主替代——每一层都有对应

```
现有互联网                    us 自主层                  Feelings 已有组件
──────────                   ──────────                ──────────────
光纤 / BGP / 海底缆          复用物理层                  不重造。KubePivot 管云 VM。

DNS（命名 → IP）             自建命名                    us 根 → SPL 锚定
                                                      → Anim busd 总线查询
                                                      不需要 DNS 服务器。

CA（信任锚 → 证书）           自建信任                   Root 锚点 7/4 Shamir
                                                      设备 FPGA TEE 硬件身份
                                                      不依赖 Let's Encrypt。

HTTP / gRPC / TLS            自建协议                   Anim busd 设备直连
                                                      PLL 主时钟广播
                                                      ESIR 帧级传输不经过 HTTP。
```

**不需要挖光纤。只是在现有光纤上换掉了 DNS、CA 和协议栈。** 现有的互联网被当作物理层——就像 TCP 跑在 IP 上，Anim 跑在 TCP 上。等 Feelings 自己的物理设备到位——Anim 直接跑在总线上，中间那层就省了。

---

## 六、分三阶段落地

### 阶段一：混合模式（现在 → 1 年）

```
目标          Feelings-Server 上线。用户通过 us.top + HTTPS 访问。
              内部服务间通信走 gRPC + 自签 mTLS。
              us 根锚点创建——7 份分片生成并分发。
              us.top ICP 备案。

基础设施      公有云 VM（通过 KubePivot 部署）
              us.top 作为唯一对外入口
              Let's Encrypt 证书作为过渡信任（→ 阶段二自建 CA 替代）
              私有 DNS 服务器——us.top 及所有子域解析自主掌控

已有组件      KubePivot     → 部署 Server / etcd / Postgres
              Axon          → 链上薪酬发放
              SPL           → 哈希链记账

成本          现有云账单 + 几台额外 VM 做分片持有者 + 私有 DNS
              月均 < ¥5000
```

### 阶段二：自建信任（1 → 3 年）

```
目标          us CA 上线。设备出厂时 FPGA TEE 写入 us 签发的根证书。
              设备之间互认不经过外部 CA。
              服务发现从 DNS 切换到 SPL 锚定 + Anim busd。

基础设施      自建 CA 基础设施——离线根 CA + 在线签发中间 CA
              分片持有者节点（7 台 VM，物理分布，至少 3 个大陆）
              Anim busd 服务发现注册中心

已有组件      Root 锚点      → 激活 7/4 分片。签发设备证书。
              Anim busd      → 主设备时钟广播 + 从设备轮询。
                              设备间通信不走 HTTP。
              SPL            → 名字 → 哈希锚定的注册表。

成本          7 台 VM + CA 基础设施 + 硬件安全模块
              月均 ¥2-5 万
```

### 阶段三：独立互联（3 → 7 年）

```
目标          us 网络内部完全自治——命名、信任、路由、协议全部自己管。
              对外保留桥接入口——普通浏览器可通过 Feelings-Web 访问。
              内部设备直接通过 Feelings-OS 裸机总线通信。
              不再依赖任何公共互联网协议层。

基础设施      自建 Feelings 设备集群（FPGA 裸机跑 Feelings-OS）
              us 网络内设备之间 PLL 时钟同步
              对外桥接网关（兼容 HTTP，内部走 busd）

已有组件      Feelings-OS    → 六守护进程。从加电到可服务 <2s。
              busd           → 总线驱动。主设备广播。从设备锁定。
              PLL timerd     → 全局主时钟。所有设备共享同一个时钟源。
              Feelings-Core  → 私人层。原始数据永不离设备。

成本          设备制造 + 裸机部署 + 运维
              百万级（可分阶段投入，随设备量产摊薄）
```

---

## 七、成本估算

```
阶段          主要投入                        月均/一次性
────          ────────                        ──────────
阶段一        云 VM + 域名 + 运维              月均 < ¥5000
              已在现有账单内

阶段二        7 台分片 VM + HSM + CA 运维      月均 ¥2-5 万
              可分阶段——先 3 台 VM 跑最小 CA

阶段三        设备制造 + 裸机 + 运维            一次性 ¥50-200 万
              随 Feelings 设备量产摊薄
              核心成本在硬件制程——FPGA + 传感器
```

---

## 八、备选名称——按优先级

### 6.1 nos

```
拼写     nos
语言     拉丁语
含义     "我们"。和 us 同义。三个字母。
选择     us 的最接近替代。比 us 多一个字母。
        比 us 更"古典"。拉丁语的内在权威感适合一个根。
```

### 6.2 NOS

```
拼写     NOS
语言     拉丁语
含义     同上。全大写。
选择     全大写版本。视觉更"商标化"。
        和古希腊语 νόος（心灵/理智）谐音——意外的双层语义。
```

### 6.3 nexus

```
拼写     nexus
语言     拉丁语
含义     "连接"、"绑在一起"。
选择     VSA 捆绑。经线穿过纬线的那个点。
        缺点——五个字母。比 us 长。
        Google Nexus 用过。但不冲突——不同领域。
```

### 6.4 syn

```
拼写     syn
语言     希腊语
含义     "一起"、"共同"。synapse 的前缀。
选择     两个神经元的突触间隙。和 us 同构。
        缺点——太通用。注册难度可能比 us 更高。
```

### 6.5 hen

```
拼写     hen
语言     希腊语（ἕν）
含义     "一"。不是加法的"一个"。是超越个体之后涌现的统一体。
选择     普罗提诺的"太一"。两根丝交叉之后——不是两件事。是一件事。
        缺点——英文里 hen = 母鸡。英文语境下会分心。
```

### 6.6 ambae

```
拼写     ambae
语言     拉丁语
含义     "两者"（阴性复数）。两个都。每一个都是。
选择     比 us 更强调双主体性。
        缺点——五个字母。发音不如 us 自然。
```

---

## 九、排除列表

| 名称 | 排除原因 |
|------|---------|
| inter | 被太多公司使用 |
| vincio | 太生僻。读不出来 |
| unum | e pluribus unum 已被美国消费 |
| soma | Brave New World 已占 |
| anima | 和 Anim 冲突 |
| psyche | 太心理学。不是 Feelings 的语义 |
| ens | 太干。太哲学 |
| coniunctim | 八个字母太长 |
| homou | 太"物理位置"。不是交叉 |

---

## 十、决策框架

```
允许两个字母 + 纯小写        → us
需要全大写                   → US 或 NOS
需要三个字母以上              → nos
需要"古典"感觉               → nos
需要"技术"感觉               → nexus
需要强调双主体性             → ambae
```

---

## 十一、当前状态

**暂定——us。** 纯小写。两个字母。域名已购入——`us.top`。等待 ICP 备案。

```
产品     Feelings
实体     us
域名     us.top ✅（已购入，2026-05-29）
根       us
标语     不需要——它自己就是。

关系     Feelings 做产品。us 做实体。us 做网络根。
        us.top 是全球唯一入口。
        三个维度。两个字母。一个域名。一套哲学。
        us 是交叉点——两根丝穿过彼此之后，
        永远改变了彼此的那个坐标。
        它不在任何一根丝上独存。
        它在两根丝交叉的那一帧——同时在两方。
```

---

*"us" 不是被选的。是所有的文档写完之后——发现它们一直在说的就是这个词。不只是名字。是根。是信任锚。是整套哲学的最小压缩包。接住。定下。阶段性验证。阶段性投入。每一层有已有组件——不需要等。已经是了。*