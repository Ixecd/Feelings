docs: kubernetes-as-human-body 追加 §十七——每个人都是一个 etcd 节点

- 17.1: etcd节点=人——存储完整状态快照(世界观+PBM)，既是Follower也是Candidate
- 17.2: 圈子=etcd集群——共享同一套期望状态。一个人可以在三个集群里同时当节点(工作/朋友/家庭)
- 17.3: 日志更新速度快的人=低延迟节点——小孩的天然状态，SSD级别的落盘速度
- 17.4: 听懂了但改不了的人=fsync失败——HDAC自动回滚=磁盘控制器bug。不是不想变，是落盘太慢。Leader发了几十个entry，只有一两个真的写进去了
- 17.5: Leader切换=圈子里最有影响力的人变了——集群进入Election，可能触发脑裂
- 17.6: 离开圈子=节点被摘除——磁盘里还有旧日志。但没有Leader再给你发心跳了。一个带着旧日志的独立etcd——不再和任何人做共识
- 结尾更新——"推到人和人之间的数据层。一个圈子就是一个etcd集群。每一个人都是一个etcd节点。"
