# FUTURE.md — Anim 架构演化实验室

> 创建日期：2026-05-20
> 作用：存放高价值、高风险、需要"重新评估时机"的架构种子
> 关联：[ROADMAP.md](Anim-ROADMAP.md)（确定性工程交付计划）

---

## 这份文档是什么 / 不是什么

`ROADMAP.md` 是 **军令状** — 写的是未来 milestone 内要交付的确定性工程。
`FUTURE.md` 是 **实验室** — 存放需要在合适时机重新评估的远景种子。

**这份文档不是承诺**：里面的每一条都不保证会实施。
**这份文档不是 backlog**：不是"想做但没时间"的待办池。
**这份文档是种子库**：当条件成熟时，重新评估、决定是否提升到 ROADMAP。

每个种子必须包含：
- **动机**：为什么这个想法值得记录
- **核心想法**：一段话讲清楚是什么
- **工程税**：诚实列出实施成本和风险
- **适用前提**：什么条件下重新评估才有意义
- **重新评估时机**：与 ROADMAP 的交点

---

## 种子库

| 编号 | 种子 | 状态 | 重新评估时机 |
|---|---|---|---|
| F1 | 张量网络 IR — 矩阵→张量的 IR 升级 | 🌱 探索中 | v0.5+ FPGA 对接稳定后 |
| F2 | Anim LSP — 编辑器语言服务 | 探索中 | v0.3+ 语法稳定后 |
| F3 | 感受包差分编译 — 只重编译变化部分 | 探索中 | v0.4+ 离线预编译上线后 |

---

## F1: 张量网络 IR — 矩阵→张量的 IR 升级

> 当前四层 IR 用矩阵变换。更精确的模型是张量网络——六阶张量沿不同维度做收缩。当 PBM 扩展到 3+ 个独立变动维度时，矩阵不够用。

### 动机

当前 IR 是矩阵变换链（FSIR×PBM→PSIR→DSIR→ESIR）。但 Feelings-MATRIX.md 已经指出：四诊合参是 7 阶张量收缩，PBM 未来要扩展到 3-4 阶（感受类型 × 生理指标 × 时间窗 × 场景类型）。矩阵是张量压扁后的二维切面——够用，但知道边界在哪。

### 核心想法

```
FSIR（二维矩阵表示）
    → 升级为 FSIR Tensor（六阶：感受维 × 点缀维 × 形状维 × 设备维 × 安全维 × 时间维）
PSIR（PBM 左乘矩阵）
    → 升级为 PSIR Tensor Contraction（PBM 4 阶 × FSIR 6 阶 → PSIR 5 阶）
```

### 工程税

1. **数据结构重构** — 全部 IR 类型从二维矩阵改为 N 阶张量
2. **性能** — 张量收缩的计算复杂度高于矩阵乘法，需 GPU/FPGA 加速
3. **可读性** — 张量网络的可调试性远低于矩阵链

### 适用前提

- [ ] v0.5 FPGA 对接完成
- [ ] PBM 确实扩展到了 3+ 维度
- [ ] 矩阵模型出现无法覆盖的场景

### 重新评估时机

**v0.5+** 重新评估。

---

## F2: Anim LSP — 编辑器语言服务

> .anim 源码的 IDE 支持：语法高亮、实时错误提示、FSIR 预览。

### 动机

感受包创作者需要实时反馈。写错一个点缀配比，编译器报错——但如果能在编辑时就看到错误，创作效率大幅提升。

### 工程税

1. 语言服务器协议（LSP）实现
2. 增量解析——文件修改后只重解析变化部分
3. VSCode / JetBrains 插件

### 适用前提

- [ ] v0.3+ 语法稳定（不再 breaking change）
- [ ] 有外部创作者使用 Anim

### 重新评估时机

**v0.3+** 重新评估。

---

## F3: 感受包差分编译 — 只重编译变化部分

> 用户修改 .anim 源码的一个点缀配比——只重新编译受影响的 IR 片段，不全量重编译。

### 动机

复杂感受包的全量交织时间可能超过 1s。差分编译可以降到 <100ms。

### 工程税

1. IR 依赖图——追踪哪个源码修改影响哪层 IR 的哪个片段
2. 增量 FSIR/PSIR/DSIR/ESIR 生成
3. 缓存失效逻辑

### 适用前提

- [ ] v0.4+ 离线预编译上线
- [ ] 全量交织时间确实超过了用户体验阈值

### 重新评估时机

**v0.4+** 重新评估。

---

## 种子状态流转

```
🌱 探索中 → ✅ 已毕业（提升到 ROADMAP）
         → ❌ 已弃用（评估后确定不做，保留决策记录）
         → ⏸️ 长期搁置（条件不成熟）
```

种子被弃用时**不删除**，保留作为"历史决策依据"。

---

## 编辑记录

```
2026-05-20  创建文档 + F1（张量网络IR）+ F2（Anim LSP）+ F3（差分编译）
```
